// import React from 'react';
// import { Alert } from 'react-native';
// import { useNavigation } from '@react-navigation/native';

// const HandleSelectionType = ({ categoryValue, subOrCourseValue }) => {
//     const navigation = useNavigation();

//     React.useEffect(() => {
//         if (categoryValue === '1' && subOrCourseValue === '1') {
//             navigation.navigate('EnglishQuestion');
//         } else {
//             Alert.alert('Invalid selection');
//         }
//     }, [categoryValue, subOrCourseValue]);

//     return null; // This screen does not render anything
// }

// export default HandleSelectionType;